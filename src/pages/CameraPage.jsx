import React from 'react';
import Camera from '../components/Camera';

const CameraPage = () => {
    return (
        <div>
            <Camera></Camera>
        </div>
    );
}

export default CameraPage;
